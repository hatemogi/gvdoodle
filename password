YOUR PASSPHRASE SHOULD BE HERE

# Config like this and replace the whole content of this file with your password
# -------------------------------------------------------------------------------
git config --local filter.crypt.clean "openssl aes-128-cbc -e -a -kfile password"
git config --local filter.crypt.smudge "openssl aes-128-cbc -d -a -kfile password"
git config --local filter.crypt.required true